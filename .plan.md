# Dostoevsky Ash Accumulation + Remaining Fixes

## Chunk 1: Volume calc + counter init + CSS (DONE)
- [x] Fix volume calculation in serverless function (months since March 2026)
- [x] Fix volume calculation in share.js (calcVolume helper)
- [x] Counter starts at 1 not 0 (first POST increments 0→1)
- [x] CSS: erased words use `visibility: hidden` instead of collapsing

## Chunk 2: Add ash canvas + mound state to dostoevsky.js
- Add ash canvas element (60px desktop, 40px mobile) at bottom of article-wrapper
- Add mound heightmap array (tracks ash height at each pixel column)
- Add `addAshAtPosition(x)` function that adds to mound heightmap
- Add `drawMounds()` function that renders organic lumpy mounds with gradient
- Wire into `initDostoevsky()` / `destroyDostoevsky()` lifecycle

## Chunk 3: Falling ash particles in dostoevsky.js
- In `disintegrateWord()`, after scatter particles, spawn 4-6 falling ash particles
- Each particle drifts downward over 2-3s with slight sideways drift
- Uses requestAnimationFrame, lands on ash canvas and calls `addAshAtPosition(x)`
- Tiny puff on landing
- Mobile: reduce particle count by 50%

## Chunk 4: Ambient ash effects + crow disturbance
- Add 3-4 tiny drifting particles per mound floating slowly upward (very faint)
- When crow flies near bottom, disturb mounds (tiny puff rises)
- Add CSS for ash canvas (pointer-events: none, positioning)

## Chunk 5: Syntax check, commit, push
- `node -c` all modified files
- Commit with descriptive message
- Push to branch
